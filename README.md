# Adly

> Módulo de Data-Buddy especializado en análisis de pauta publicitaria.
> Cliente 0: Camí. Python. Stack agnóstico al proveedor de IA.

## Setup

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env
python scripts/cli.py
```
